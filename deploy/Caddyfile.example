# Keep the existing browser-trusted HTTPS hostname. Direct IP addresses do not
# qualify for public ACME certificates and must not be replaced with HTTP.
<PUBLIC_HOSTNAME> {
	encode zstd gzip

	handle_path /api/* {
		reverse_proxy 127.0.0.1:3001
	}

	handle {
		reverse_proxy 127.0.0.1:3000
	}
}
